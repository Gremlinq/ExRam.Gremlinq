<!--
Replace this comment with one to three sentences saying what changed and why.

Everything above the first "##" heading is the source for this change's entry in the
release notes, so write it for someone reading the changelog, not for someone reading
the diff: state the intent and the effect, not the file or line count.

The sections below are optional. Delete what you do not need.

A CI check rejects pull requests whose description is missing or barely longer than the
title. Chores that should not appear in the release notes -- CI tweaks, dependency
bumps, refactorings with no user visible effect -- can carry the "skip-changelog" label
instead, which exempts them from the check.
-->

## Changes

<!-- Optional. One bullet per project or area, e.g. "**Core**: ..." -->

## Notes

<!-- Optional. Public API impact, breaking changes, benchmark numbers, follow-ups. -->
