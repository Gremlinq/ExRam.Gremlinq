{
  RequestId: Guid_1,
  Operation: eval,
  Processor: ,
  Arguments: {
    bindings: {
      _a: Person,
      _b: Name
    },
    gremlin: g.V().hasLabel(_a).has(_b).filter({it.get().property('Name').value().length() == 2})
  }
}