{
  RequestId: Guid_1,
  Operation: eval,
  Processor: traversal,
  Arguments: {
    aliases: {
      g: g
    },
    bindings: {
      _a: Person,
      _b: Age
    },
    gremlin: g.V().hasLabel(_a).values(_b).fold().mean(local)
  }
}