{
  RequestId: Guid_1,
  Operation: eval,
  Processor: traversal,
  Arguments: {
    aliases: {
      g: g
    },
    bindings: {
      _a: Company,
      _b: Locations
    },
    gremlin: g.V().hasLabel(_a).properties(_b).properties().value()
  }
}