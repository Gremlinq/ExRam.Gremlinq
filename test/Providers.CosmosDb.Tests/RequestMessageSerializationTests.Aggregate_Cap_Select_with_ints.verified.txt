{
  RequestId: Guid_1,
  Operation: eval,
  Processor: traversal,
  Arguments: {
    aliases: {
      g: g
    },
    bindings: {
      _a: Person,
      _b: _a
    },
    gremlin: g.V().hasLabel(_a).count(local).aggregate(_b).cap(_b).select(_b)
  }
}